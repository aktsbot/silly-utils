/**
 * Get last element in array
 * @param {Array} array
 * @return {*} - last element in array
 */
let last = array => {
  let lastElement = array[array.length - 1]
  return lastElement
}

module.exports = last
